{% extends 'base.html.twig' %}

{% block title %}Ajouter un mot clé
{% endblock %}

{% block body %}


	<div class="container">
		<h1 class="mt-4">Ajouter un mot clé</h1>

		{{ form_start(keywordForm) }}
		{{ form_row(keywordForm.name) }}
		{{ form_row(keywordForm.slug) }}


		{{ form_end(keywordForm) }}


	</div>
{% endblock %}
