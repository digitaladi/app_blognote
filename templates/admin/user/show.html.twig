{# cette page étend le twig base.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}blognote
{% endblock %}

{% block body %}

	<div class="container mt-4">
		<div class="list-group">
			<a href="#" class="list-group-item list-group-item-action active">
				<h4>
					{{ user.username }}</h4>
			</a>
			<a href="#" class="list-group-item list-group-item-action">{{ user.email }}</a>
			<a href="#" class="list-group-item list-group-item-action disabled">actif</a>
		</div>


		<ul class="list-group mt-2">
			<li class="list-group-item d-flex justify-content-between align-items-center">
				Nombres d'astuces
				<span class="badge bg-primary rounded-pill">{{ user.getTricks|length  }}</span>
			</li>
			<li class="list-group-item d-flex justify-content-between align-items-center">
				Nombre de commentaires
				<span class="badge bg-primary rounded-pill">{{ user.getComments|length  }}</span>
			</li>
			<li class="list-group-item d-flex justify-content-between align-items-center">
				Nombre de notations
				<span class="badge bg-primary rounded-pill">{{ user.getRatings|length  }}</span>
			</li>
		</ul>
	</div>
{% endblock %}
