<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container">
		<a class="navbar-brand" href="{{ path('app_home') }}">
			<strong>Blog</strong>Note</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul
				class="navbar-nav me-auto">
				{#  #}
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('app_profil_trick_trickday') }}">A propos
						<span class="visually-hidden">(current)</span>
					</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_profil_trick_index') }}">Les astuces</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_profil_trick_trickday') }}">Astuce du jour</a>
				</li>


			</ul>

			<div class="account-left-wrapper">
				<ul class="navbar-nav me-auto">
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">


							{# comment utiliser ux symfony icon #}
							{# tabler => le prefix #}
							{# user-circle: le nom de l'icone #}
							{# user-circle: le nom de l'icone #}
							{# {height:'30px', width:'30px'} :  la taille de l'icone #}
						{{ ux_icon('tabler:user-circle', {height:'30px', width:'30px'}) }}


						</a>
							<div class="dropdown-menu"> <a class="dropdown-item" href="{{ path('app_register') }}">S'inscrire</a>
							<a class="dropdown-item" href="{{ path('app_login') }}">Se connecter</a>
							<a class="dropdown-item" href="{{ path('home_admin') }}">Administration</a>
							<a class="dropdown-item" href="{{ path('app_profil_trick_trickday') }}">Mon compte</a>

							<div class="dropdown-divider"></div>
							<a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a>
						</div>
					</li>
				</ul>
			</div>

		</div>
	</div>
</nav>


<!-- 

<nav
	class="navbar">
	 la fonction asset de twig a comme paramétre le chemin et  pointe vers le dossier public
	<a href="{{ path("app_home") }}"><img width="50px" src="{{ asset("assets/images/blog.png") }}" alt=""></a>
	<ul>
		<li>Accueil</li>
		<li>Notes</li>
		<li>Catégories</li>
		<li>Auteurs</li>
		<li>Contact</li>
		<li>Recherche</li>
	</ul>

</nav>
-->
